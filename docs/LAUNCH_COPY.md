# Launch Copy Pack

## GitHub Repo Subtitle

Production-style local RAG + controllable agents with grounded citations, out-of-scope routing, and notebook-to-module workflow.

## Reddit Title Ideas

1. Proferio: End-to-End Local RAG + Agent Kit (Grounded Citations + OOS Routing)
2. I built a production-style local RAG starter kit that refuses hallucinations
3. Notebook-first local RAG that actually ships: retrieval, agent, eval, and live UI

## Reddit Post Body (template)

Built this for people tired of fragmented local RAG tutorials.

What it does:
- Local-first RAG with `ollama` support
- Multi-query retrieval + rerank + diversity filtering
- Out-of-scope detection (returns status, avoids fake answers)
- ReAct-style controllable agent extension
- Smoke test + benchmark scripts
- Gradio demo with answer + source trace + status panel

If useful, happy to add:
- Better reranker training recipes
- More multimodal examples
- Additional benchmark presets by hardware tier

## X Post

Shipped: **Proferio**\n
A production-style, notebook-first local RAG + agent starter kit with:\n
- grounded citations\n
- out-of-scope routing\n
- retrieval + rerank + eval\n
- smoke tests + live UI\n
Built for real workflows, not toy demos.\n
#LocalLLM #RAG #AIEngineering
